/**
 * Navbar.jsx — Re-exports ResponsiveNav for backward compatibility.
 *
 * All existing imports of `Navbar` continue to work without changes.
 * The actual implementation now lives in `./layout/ResponsiveNav.jsx`.
 */
export { default } from './layout/ResponsiveNav'
